import { PersonStanding } from "lucide-react";

export function MenuTitle() {
  return (
    <h4 className="flex items-center">
      <PersonStanding className="text-primary" size={30} />
      SupportMe
    </h4>
  );
}
